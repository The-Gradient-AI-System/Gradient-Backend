# JWT / Auth
SECRET_KEY=your-secret-key-for-jwt
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_HOURS=2

# OpenAI
OPENAI_API_KEY=sk-your-openai-key
OPENAI_MODEL=gpt-4.1-mini

# CORS (comma-separated origins; for production add your Vercel frontend URL)
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Google Sheets (optional, for sheet sync)
# SPREADSHEET_ID=your-google-sheet-id

# Gmail/Sheets: web OAuth (for Render) — set these, then open GET /gmail/auth in browser to log in
# BACKEND_URL=https://gradient-backend-xb7i.onrender.com
# GOOGLE_CLIENT_ID=xxx.apps.googleusercontent.com
# GOOGLE_CLIENT_SECRET=xxx
# Add in Google Cloud Console → Credentials → OAuth 2.0 → Authorized redirect URIs:
#   https://gradient-backend-xb7i.onrender.com/gmail/oauth2callback
# Optional: after first login, copy token to GMAIL_TOKEN_JSON so it persists across restarts
# GMAIL_TOKEN_JSON={"token":"...","refresh_token":"...","token_uri":"...","client_id":"...","client_secret":"...","scopes":[...]}

# Optional
# AI_DEBUG=false
# COMPANY_SEARCH_ENABLED=true
