# Render.com Blueprint – Gradient Backend (FastAPI)
# https://docs.render.com/blueprint-spec

services:
  - type: web
    runtime: docker
    name: gradient-backend
    region: frankfurt
    plan: free
    envVars:
      - key: PORT
        value: 8000
      - key: SECRET_KEY
        sync: false
      - key: OPENAI_API_KEY
        sync: false
      - key: CORS_ORIGINS
        sync: false
